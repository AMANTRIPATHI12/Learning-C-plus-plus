C++ is a general purpose programming language created by Bjarne Stroustrup in early 1980s.
It is an extention of C language that is why it is called C++. 
This language is works very closely with the memory that is why it is used to build everything from OS, games, browsers, to emebeded systems.
C++ supports different programmming styles like procedural, OOps, and generic.
entry point in C++ is the main method and c++ is a case sensitive language so capital and small case words are treated as different.
C++ ignores the empty spaces given to it.
<iostream> is a standard library provided by C++. It is a collection of codes provided by C++ to make out life easier. Almost every language gives its library.
IO Stream means input output stream used for input and output in the programming.
methods can have "0" or more parameters at a time.
cout means character out not console out.
return zero tells the os that the program terminates correctly if we return any value other than 0 than it means our program encountered an error.
always end statements with a semi colon;


1. procedural programmming 